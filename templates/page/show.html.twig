<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
</head>
<body>
    <h1>{{ page.title }}</h1>
    <div>{{ page.content|raw }}</div>
    <p><a href="/">Home</a></p>
</body>
</html>
